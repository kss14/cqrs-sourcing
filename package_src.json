{
  "name": "node-demo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "npx nodemon server.js",
    "build": "pnpm install && docker-compose up --build",
    "dc_only": "docker-compose up --build",
    "kill-port": "kill -9 $(lsof -t -i:$npm_config_port)",
    "command-kill-port": "echo \"npm run kill-port --port=3000\"",
    "test": "echo \"Error: no test specified\" && exit 1",
    "preinstall": "node merge.js && prettier-package-json --write package.json"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "glob": "^8.0.3",
    "nodemon": "2.0.2",
    "npm-run-all": "^4.1.5"
  },
  "dependencies": {
    "merge-package-json": "^0.1.3"
  }
}
